import {
  makeSuite,
  TestEnv,
} from "../protocol-v2/test-suites/test-baave/helpers/make-suite";

import { HardhatRuntimeEnvironment } from "hardhat/types";

makeSuite("Dummy Test", (testEnv: TestEnv) => {
  it("", async () => {
    // Empty Test
  });
});
